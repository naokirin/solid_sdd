\* TLC config for ClaimCoordinator (NFR1: at most one worker claims a given
\* job, even under concurrent/interleaved claim attempts)
CONSTANTS
  Workers = 3
  Jobs = 2

SPECIFICATION Spec

\* Safety (NFR1's own requirement) + end-state sanity check.
INVARIANT Inv
INVARIANT FinalOK

\* Liveness: every job is eventually claimed under fair scheduling.
PROPERTY EventuallyAllJobsClaimed

\* Allow intentional stuttering once Done (Terminating action).
CHECK_DEADLOCK FALSE
