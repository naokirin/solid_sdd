{
  "version": "1",
  "human_gate": {
    "required": false,
    "reason": "User instruction already settled framing: single additive operation (`pow`) on the existing `POST /calculate` endpoint, explicitly no new precondition, explicit out-of-scope list (memory ops, existing-op behavior, new routes). Stack is `repo_existing` (TypeScript/OpenAPI/OCL/Vitest) with no conflict. No new security/money NFR. §7 items are non-blocking deferrals (overflow/special-value semantics, response shape), consistent with prior-change precedent."
  },
  "confidence": "high",
  "decisions_to_confirm": []
}
