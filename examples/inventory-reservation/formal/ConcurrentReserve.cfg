\* TLC config for ConcurrentReserve (last-unit / never-negative stock)
CONSTANTS
  Clients = 2
  Stock = 1

SPECIFICATION Spec

INVARIANT Inv
INVARIANT FinalOK

\* Allow intentional stuttering when Done (Terminating action).
CHECK_DEADLOCK FALSE
