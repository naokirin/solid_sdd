{
  "version": "1",
  "change_id": "add-list-sort-expires",
  "summary": "Follow-on N=1 WorkPlan: one co-delivered property slice adds expiresAt-ascending sort to the existing authorized soft-hold collection list while keeping UnauthorizedError and full-dump/AuthZ/visibility/DTO unchanged; no vocabulary-only foundation (list contracts already exist).",
  "acceptance_of_whole": "Scenario: Soft-hold collection list is sorted by expiresAt ascending with AuthZ unchanged\n  Given the inventory reservation service for clients\n  When clients exercise authorized soft-hold collection list and unauthorized list\n  Then authorized list returns currently visible soft-holds with non-decreasing expiresAt (ascending; equal expiresAt any stable order) without mutating stock or holds\n  And the dump remains unfiltered LookupResponse-equivalent items with no filters, paging, or DTO field changes\n  And unauthorized list fails with UnauthorizedError without mutating stock or holds (POL-OPAQUE-PRINCIPAL-AUTHZ)\n  And openapi/openapi.yaml and/or contracts/**/*.ocl plus passing tests/contracts/** make authorized sorted-list success and UnauthorizedError on list checkable\n  And prior reserve/release/expire/lookup/concurrent and list AuthZ/visibility/DTO surfaces remain unbroken",
  "confidence": "high",
  "human_gate": {
    "required": false,
    "reason": "Follow-on sort-only delta on existing GET /reservations; Brief settled (ties any stable order; AuthZ/visibility/full-dump Means reused); open_questions are non-blocking deferrals to judge/apply; single co-delivered Scenario covers all R*/SC* without ambiguity."
  },
  "items": [
    {
      "id": "W1",
      "intent": "Extend existing GET /reservations / Reservation::list so authorized opaque principals receive currently visible soft-holds ordered by expiresAt ascending (ties any stable order) without mutating stock or holds, without filters/paging/DTO changes; keep UnauthorizedError on unauthorized list unchanged; extend OpenAPI and/or OCL + Vitest so sorted success and UnauthorizedError remain checkable.",
      "acceptance_criterion": "Scenario: Authorized list returns visible soft-holds sorted by expiresAt ascending without mutating AuthZ or dump shape\n  Given a reservation service with zero or more currently visible soft-holds\n  And an authorized opaque principal\n  When the principal lists soft-holds\n  Then the service returns the currently visible soft-holds ordered by expiresAt ascending (earlier first)\n  And equal expiresAt may appear in any stable order\n  And the response remains one unfiltered full dump of LookupResponse-equivalent items with no paging and no DTO field changes\n  And stock and holds are unchanged\n  And when an unauthorized caller attempts to list soft-holds the operation fails with UnauthorizedError without mutating stock or holds\n  And openapi/openapi.yaml and/or contracts/**/*.ocl plus passing tests/contracts/** make authorized sorted-list success and UnauthorizedError on list checkable\n  And prior reserve, release, expire, lookup, concurrent, and list AuthZ/visibility/DTO surfaces remain unbroken",
      "covers": [
        "R1",
        "R2",
        "SC1",
        "SC2",
        "SC3"
      ],
      "touches": [
        "openapi/openapi.yaml",
        "contracts/Reservation.ocl",
        "tests/contracts/reservation.test.ts",
        "src/reservation.ts",
        "src/server.ts"
      ],
      "feature_path": "requirements/reservation-list-sort-expires.feature",
      "scenario_name": "Authorized list returns visible soft-holds sorted by expiresAt ascending without mutating AuthZ or dump shape",
      "depends_on": [],
      "status": "done",
      "confidence": "high",
      "human_gate": {
        "required": false,
        "reason": "Additive expiresAt-ascending sort on existing list; AuthZ/UnauthorizedError and dump shape unchanged; OpenAPI prose vs OCL/tests alone deferred to judge/apply."
      }
    }
  ]
}
