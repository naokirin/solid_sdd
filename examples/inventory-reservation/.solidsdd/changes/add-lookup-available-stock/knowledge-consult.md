# Knowledge consult — add-lookup-available-stock

mode: consult  
change_id: add-lookup-available-stock  
kg_build: ok (`.solidsdd-cache/kg.db`; nodes from Brief/Gherkin imports; no knowledge markdown nodes)

## Applicable policies / concepts / decisions

**None**

`knowledge/` contains only `.gitkeep`. No durable policies, concepts, decisions, patterns, or lessons are present for this project yet. `solidsdd-kg scope` returns no applicable policies.

## Suggested citations (Brief assumptions / constraints)

None — no knowledge node ids to cite.

## Gaps

- `knowledge/` is empty (no harvested durable knowledge yet).
- `.solidsdd/kg/` is present (`config.yaml`, `schema.yaml`, `links.yaml`); CLI build succeeded.
- Intake / Brief should proceed without knowledge constraints; harvest later if durable norms emerge from this change.
