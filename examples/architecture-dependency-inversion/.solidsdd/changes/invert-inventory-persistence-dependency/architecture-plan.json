{
  "version": "1",
  "status": "changed",
  "change_id": "invert-inventory-persistence-dependency",
  "summary": "Projected from workspace.dsl + invariants.yaml for change 'invert-inventory-persistence-dependency': 3 module(s), 2 dependency edge(s), 1 constraint(s).",
  "modules": [
    {
      "id": "inventory",
      "responsibility": "Own available stock per SKU; expose stock read/adjust operations.",
      "owns": [
        "Stock"
      ],
      "public": [
        "InventoryService"
      ]
    },
    {
      "id": "inventory-repository-port",
      "responsibility": "Persistence abstraction that Inventory depends on and infrastructure adapters implement."
    },
    {
      "id": "postgres-store",
      "responsibility": "Concrete PostgreSQL persistence for inventory rows."
    }
  ],
  "dependencies": [
    {
      "from": "inventory",
      "to": "inventory-repository-port",
      "reason": "Reads and writes stock through the persistence port.",
      "kind": "runtime"
    },
    {
      "from": "postgres-store",
      "to": "inventory-repository-port",
      "reason": "Implements the persistence port with PostgreSQL.",
      "kind": "data"
    }
  ],
  "constraints": [
    {
      "type": "forbid_dependency",
      "from": "postgres-store",
      "to": "inventory",
      "reason": "Infrastructure must depend only on the persistence port, not directly on the Inventory domain module, so persistence implementations can be swapped without the domain knowing about them."
    }
  ]
}
