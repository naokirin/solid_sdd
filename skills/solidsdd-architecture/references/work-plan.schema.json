{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://solid-sdd.dev/schemas/work-plan.schema.json",
  "title": "WorkPlan",
  "description": "Output of solidsdd.decompose: requirement sliced into verifiable work items for solidsdd-run (distinct from ApplicationPlan). Acceptance criteria are Gherkin Scenarios. Each item covers ChangeBrief scope ids via covers[].",
  "type": "object",
  "additionalProperties": false,
  "required": ["version", "items"],
  "properties": {
    "version": {
      "type": "string",
      "const": "1"
    },
    "change_id": {
      "type": "string",
      "pattern": "^[a-z0-9]+(-[a-z0-9]+)*$",
      "minLength": 3,
      "maxLength": 48,
      "description": "Optional but recommended: must match the change directory / Brief change_id when present."
    },
    "summary": {
      "type": "string",
      "description": "Short human-readable summary of the decomposition."
    },
    "acceptance_of_whole": {
      "type": "string",
      "description": "Overall completion condition checked by the final solidsdd-verify after all items. Prefer a Gherkin Scenario or an explicit conjunction of scenario names."
    },
    "human_gate": {
      "$ref": "#/$defs/human_gate"
    },
    "confidence": {
      "$ref": "#/$defs/confidence"
    },
    "items": {
      "type": "array",
      "minItems": 1,
      "items": {
        "$ref": "#/$defs/item"
      }
    }
  },
  "$defs": {
    "confidence": {
      "type": "string",
      "enum": ["low", "medium", "high"]
    },
    "human_gate": {
      "type": "object",
      "additionalProperties": false,
      "required": ["required", "reason"],
      "properties": {
        "required": { "type": "boolean" },
        "reason": { "type": "string" }
      }
    },
    "item": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "id",
        "intent",
        "acceptance_criterion",
        "covers",
        "depends_on",
        "status"
      ],
      "properties": {
        "id": {
          "type": "string",
          "description": "Stable id within this WorkPlan (e.g. W1)."
        },
        "intent": {
          "type": "string",
          "description": "Change intent passed into one solidsdd-loop run for this slice."
        },
        "acceptance_criterion": {
          "type": "string",
          "description": "Exactly one Gherkin Scenario (or one independently checkable Scenario Outline slice) with Given/When/Then. Not free-form prose alone."
        },
        "covers": {
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "string",
            "pattern": "^[A-Z][A-Z0-9]*[0-9]+$"
          },
          "description": "ChangeBrief scope ids this item covers (in_scope R*, success_criteria SC*). Must not list out_of_scope X* ids."
        },
        "touches": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Primary edit paths this slice contends on (e.g. openapi/openapi.yaml). solidsdd-run serializes ready items whose touches intersect."
        },
        "feature_path": {
          "type": "string",
          "description": "Optional path to requirements/**/*.feature containing this scenario."
        },
        "scenario_name": {
          "type": "string",
          "description": "Optional Scenario name matching the Feature file."
        },
        "depends_on": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Other item ids that must be done before this item is ready."
        },
        "status": {
          "type": "string",
          "enum": ["pending", "ready", "done", "blocked"]
        },
        "confidence": {
          "$ref": "#/$defs/confidence"
        },
        "human_gate": {
          "$ref": "#/$defs/human_gate"
        }
      }
    }
  }
}
